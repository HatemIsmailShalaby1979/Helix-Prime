# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.Python
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
*.cover

# Virtual environments — DO NOT TRACK (see C0 hygiene fix 2026-08-27)
.venv/
.venv*/
venv/
env/
ENV/
.env/
.conda/

# Node
node_modules/
dist/

# Logs & local databases (C2/C3: do not commit workflow.db)
*.log
*.sqlite
*.sqlite-journal
*.db
*.db-journal
*.db-wal
*.db-shm
control_plane/workflow.db
control_plane/*.db
!engines/wfm/src/data/*.csv
cockpit/memory/cognitive_log.jsonl
cockpit/memory/cognitive_log.sqlite

# OS
.DS_Store
Thumbs.db

# Env secrets
.env
.env.local
.env.*.local
!.env.example
# Local session cookie jar (curl/wget scratch from a loopback dev run)
cookies.txt

# Tauri / Rust
src-tauri/target/
src-tauri/releases/

# Evidence & generated artifacts (replace with evidence/ convention)
evidence/*
!evidence/README.md
!evidence/.gitkeep
.pytest-tmp/
*.tmp
# C3 observability & audit (local-first, do not commit)
observability/logs.jsonl
security/audit.db
security/audit.db-*
*.jsonl

# Local demo media kept out of source (use release assets instead)
marketing/assets/*.mp4

# Archify scratch — regenerate from specs/*.json with the archify CLI. The
# deliverables are the diagram .html files and their specs; these are the
# per-run working directories and the visual-check renders they produced.
.archify-delivery-*/
*.visual-check.*

# IDE / assistant tooling — project-local data, not source (Phase 1 W0)
.workbuddy-ai/
.tmp/
.claude/worktrees/
