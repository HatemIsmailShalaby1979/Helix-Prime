# User Data Folder

This is a templated folder where you would put your configuration objects, if you haven't created any.

If you have configuration files you wish to bring under management, change the path in:

`project_root/.omni/config.json: CONFIGURATIONS_FOLDER_PATHS`

to point to the correct folder (then delete this folder).
