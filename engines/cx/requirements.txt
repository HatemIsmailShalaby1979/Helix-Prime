# CX Churn Sentinel — Python dependencies
# Local-first: pandas/numpy/scipy are the only hard requirements.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# Optional: live DB extraction (sql_extractor degrades gracefully without it)
# psycopg2-binary>=2.9
# SQLAlchemy>=2.0

# Optional: chart rendering for local exploration
# plotly>=5.15
