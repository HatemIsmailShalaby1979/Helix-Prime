# DEPRECATED C0 COMPATIBILITY SHIM
# CX runtime dependencies are canonical in ../../requirements.txt.
# Install from the repository root: python -m pip install -r requirements.txt
# This file intentionally declares no packages; do not add dependencies here.
