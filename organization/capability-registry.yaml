# Helix Prime — CANONICAL Capability Registry (Codex C1a) — ONE SOURCE OF TRUTH
# Canonical: organization/capability-registry.yaml (this file)
# Mirrors (GENERATED ARTIFACTS — do not hand-edit):
#   - contracts/capabilities.yaml (YAML view)
#   - organization/capabilities.json (JSON view)
# Regenerate the mirrors with: python scripts/sync_capability_mirrors.py
# They are written only by that script; CI runs it with --check and
# tests/test_capability_registry_drift.py validates the committed copies.
# Nothing reads the mirrors at runtime — organization/capability_registry.py loads
# this file directly. Agents' capabilities are canonical in
# organization/role-catalog.yaml (owned_capabilities) and merged at runtime.
# No capability is silently ambiguous — duplicates fail closed.
schema_version: "1.0"
generated: "2026-08-27"
classification: "C1A_CANONICAL"
canonical_source: "organization/capability-registry.yaml"

# Engine capabilities: capability -> engine name (must match engines/* names)
engine_capabilities:
  # WFM Forecasting (Erlang C)
  erlang_c: "WFM Forecasting"
  variance_analysis: "WFM Forecasting"
  wfm_forecast_engine: "WFM Forecasting"
  data_pipeline: "WFM Forecasting"
  # RTA Command Center
  rta_adherence: "RTA Command Center"
  schedule_tracking: "RTA Command Center"
  adherence_calculation: "RTA Command Center"
  # CX Churn Sentinel
  churn_risk_scoring: "CX Churn Sentinel"
  risk_scoring: "CX Churn Sentinel"
  sentiment_analysis: "CX Churn Sentinel"
  kpi_aggregation: "CX Churn Sentinel"
  alert_dispatch: "CX Churn Sentinel"
  # B2B Onboarding
  b2b_onboarding: "B2B Onboarding"
  sop_generation: "B2B Onboarding"
  client_profiling_b2b: "B2B Onboarding"
  notion_integration: "B2B Onboarding"
  # Personnel Engine (use _engine suffix to avoid collision with HR GM agent capabilities)
  talent_acquisition_engine: "Personnel Engine"
  hiring_pipeline_engine: "Personnel Engine"
  workforce_planning_engine: "Personnel Engine"
  # CRM Engine
  sales_pipeline: "CRM Engine"
  customer_support: "CRM Engine"
  crm_operations: "CRM Engine"

# Agent capabilities are NOT duplicated here — they are canonical in role-catalog.yaml.
# This registry is for engine lookup; agent lookup is built dynamically from role catalog.
# For C1a, the Python registry (organization/capability_registry.py) merges both sources
# and enforces determinism + fail-closed for unknown/ambiguous.
