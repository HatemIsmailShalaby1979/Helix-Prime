[pytest]
markers =
    smoke: marks tests as smoke tests requiring live Ollama service
    unit: fast, no external dependencies
    integration: requires engines or Ollama
testpaths =
    tests
addopts = -r a --strict-markers
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
