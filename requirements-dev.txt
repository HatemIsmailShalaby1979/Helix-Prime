# Helix Codex OS — development, test and release tooling.
# Runtime dependencies live in requirements.txt; this file is additive only.
#
#   python -m pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# ── Test ────────────────────────────────────────────────────────────────────
pytest>=7.4,<10
pytest-timeout>=2.2
pytest-cov>=4.1

# ── Lint / format ───────────────────────────────────────────────────────────
ruff==0.1.15
flake8>=6.0
pre-commit>=3.5

# ── Types ───────────────────────────────────────────────────────────────────
mypy>=1.8
types-PyYAML>=6.0
